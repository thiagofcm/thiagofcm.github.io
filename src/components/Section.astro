---
interface Props {
  title: string;
  id: string;
}

const { title, id } = Astro.props;
---

<div class="max-w-[900px] pt-16" id={id} data-section>
  <h3 class="text-2xl font-bold">{title}</h3>

  <slot />
</div>
